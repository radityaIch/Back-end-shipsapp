// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // directUrl = env("DIRECT_URL")
}

model Vendor {
  id              String       @id @default(cuid())
  nama            String
  alamat          String
  pic             String
  no_telp         String
  email           String
  website         String?
  pricelist_pdf   String?
  list_pengiriman Pengiriman[]
}

enum SatuanEstimasiWaktu {
  Jam
  Hari
  Minggu
  Bulan
  Tahun
}

model Order {
  id               String      @id @default(cuid())
  pengiriman_id    String
  pengiriman       Pengiriman? @relation(fields: [pengiriman_id], references: [id], onDelete: Cascade)
  panjang          Float
  lebar            Float
  tinggi           Float
  koli             Int
  biaya_pengiriman Int
}

model Pengiriman {
  id                    String              @id @default(cuid())
  provinsi_awal         String
  provinsi_tujuan       String
  kabupaten_awal        String
  kabupaten_tujuan      String
  kecamatan_awal        String
  kecamatan_tujuan      String
  min_charge            Int
  price                 Int
  satuan_estimasi_waktu SatuanEstimasiWaktu
  estimasi_tercepat     Int
  estimasi_terlama      Int
  jarak_tempuh          String
  Vendor                Vendor?             @relation(fields: [vendorId], references: [id], onDelete: Cascade)
  vendorId              String?
  Order                 Order[]
}

model Pelabuhan {
  id                 String          @id @default(cuid())
  kode_pelabuhan     String?         @db.VarChar(255)
  url_gambar         String          @db.VarChar(255)
  nama_pelabuhan     String          @db.VarChar(255)
  lokasi_pelabuhan   LokasiPelabuhan @relation(fields: [lokasi_pelabuhanId], references: [id])
  lokasi_pelabuhanId String
  shipagents         Shipagent[]
  bunker_services    BunkerService[]
  marine_services    MarineService[]
  logistiks          Logistik[]
  truckings          Trucking[]
}

model LokasiPelabuhan {
  id        String      @id @default(cuid())
  nama      String      @unique @db.VarChar(255)
  pelabuhan Pelabuhan[]
}

model Shipagent {
  id         String      @id @default(cuid())
  nama       String      @unique
  pelabuhans Pelabuhan[]
}

model BunkerService {
  id         String      @id @default(cuid())
  nama       String      @unique
  pelabuhans Pelabuhan[]
}

model MarineService {
  id         String      @id @default(cuid())
  nama       String      @unique
  pelabuhans Pelabuhan[]
}

model Logistik {
  id         String      @id @default(cuid())
  nama       String      @unique
  pelabuhans Pelabuhan[]
}

model Trucking {
  id         String      @id @default(cuid())
  nama       String      @unique
  pelabuhans Pelabuhan[]
}
